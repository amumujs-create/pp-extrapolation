# 관측 이력 → 함수 관계 분포: 개선 구현 결과

## 결정

관측 이력을 이용하는 회귀형 함수 분포 후보에서 TRAIN 내부 외삽 평균 오차 개선을 확인했다. 그러나 복잡한 신경망 후보는 악화했고 예측구간 coverage도 부족했다. 기존 PP-X는 교체하지 않는다. Engression 초과, 전체 데이터 커버리지, 학술적 노벨티를 입증한 결과가 아니다.

## 구현

`src/pp_extrapolation/prefix_relation_encoder.py`에 신규 모듈을 추가했다. 각 유닛의 현재 7개 관측 특징, 현재까지의 누적 평균 7개, 관측 수의 로그를 사용한다. 시간순 정렬 후 누적하므로 현재 행 뒤의 관측이나 query RUL 정답은 읽지 않는다. 이력은 전달된 데이터의 첫 행에서 시작한다. 실제 원자료 전체 이력을 복원한 시계열 인코더는 아니다.

소스 유닛의 함수 프라이어 계수 a,b,c를 unit-balanced ridge로 예측한다. 함수는 기존 `RUL(m)=integral[0,m] exp(a+b*h+c*h²)dh`다. 한 계수 표본은 전체 건강 구간의 일관된 함수를 정의한다. 단, 관측 이력이 바뀌어 다시 조건화하면 계수 분포도 바뀐다.

계수 공분산은 소스 유닛 하나씩 제외한 회귀 잔차에서 추정하고 대각 shrinkage와 분산 하한을 적용한다. 이는 경험적인 계수 오차 근사이며, 관측 잡음·함수족 오지정까지 포괄하는 보정된 예측구간이 아니다. 회귀 계수의 표적 역시 추정값이지 물리적 참값이 아니다.

주 후보에는 ridge 위의 작은 잔차 신경망(은닉 16, tanh, 계수 보정 범위 제한)을 추가했다. 소스 RUL에 대한 energy loss와 인접 관측 이력 사이의 계수 변화 페널티를 함께 학습한다. 변화 페널티 제거 대조군도 동일하게 실행했다. 학습은 고정 160 step이며 제외 유닛 query로 checkpoint나 하이퍼파라미터를 선택하지 않았다. 인접 계수 평활화가 실제 레짐 전환을 포착하는 구조라고 주장하지 않는다.

## 평가 범위

- 기존 MICH TRAIN만 사용. 외부 VAL·테스트는 읽지 않음.
- 3개 유닛 fold × 건강 cutoff 0.6/0.8. donor 유닛과 query 유닛이 다르고, donor 건강 구간 전체가 query보다 높음.
- 함수 bank와 encoder 모두 donor 초기 구간만으로 학습. donor의 후반 RUL을 슬쩍 추가하지 않음.
- 유닛별 prefix 특징은 소스와 query에서 각각 계산. query 유닛의 이전 query 관측은 이후 query 시점에 사용 가능하지만, 미래 query 관측 및 RUL은 사용하지 않음.
- 신경망 seed 42/43/44, 고정·회귀 대조군은 결정적 1회. 6개 에피소드가 겹치므로 독립 반복이나 유의성 검정으로 해석하지 않음.
- 사전 저장한 주 후보 통과 조건: 모든 대조군보다 에피소드 평균 유닛 MSE 2% 이상 개선, 최악 유닛 MSE가 균일 공유보다 나쁘지 않음, 명목 90% 구간 coverage 90% 이상.

## 결과

MSE는 정규화 RUL의 유닛별 평균 제곱오차를 각 에피소드에서 계산한 뒤 6개를 평균했다. 신경망 점예측은 3개 seed 평균이다. coverage는 각 seed의 개별 구간에 대한 행 coverage를 seed 및 에피소드에서 평균한 값이며, ensemble 혼합분포의 coverage는 아니다.

| 후보 | 유닛 MSE ↓ | 명목 90% 구간 coverage |
|---|---:|---:|
| 균일 함수계수 분포 공유 | 0.557827 | 78.56% |
| 고정 관측 유사도 분포 공유 | 0.422310 | 82.61% |
| **관측 이력 회귀형 분포** | **0.309736** | **60.04%** |
| 잔차 신경망, 일관성 손실 없음 | 0.706714 | 59.00% |
| 잔차 신경망 + 일관성 손실: 주 후보 | 0.706228 | 58.94% |

회귀형은 고정 유사도 대비 평균 MSE 약 26.7%, 균일 공유 대비 약 44.5% 개선이다. 다만 6개 에피소드 중 하나에서는 고정 유사도보다 나쁘므로 전 구간 우위가 아니다. 더 긴 초기 구간인 cutoff 0.8에서 특히 큰 개선이 있었고, 관측 구간에 따른 편차가 남는다.

회귀형은 구간 coverage가 크게 부족하여 성공 후보로 승격하지 않는다. 신경망은 회귀형보다 크게 악화했고 일관성 손실의 추가 효과도 작다. 주 후보의 사전 통과 조건은 실패했다. 신경망·일관성 손실을 성능이나 노벨티 기여로 내세울 근거가 없다.

균일 공유 결과는 함수 ‘계수 분포의 표본 평균’이다. 이전 진단의 결정적 계수 평균 또는 함수값 균일 평균과는 다른 계산이므로 수치를 직접 혼동하지 않는다.

## 해석과 후속 경계

이 결과는 작은 데이터에서 단순한 관측→함수 관계 예측이 복잡한 잔차 학습보다 나을 수 있다는 개발 증거다. 반면 평균적으로 유용한 관계 신호가 있다는 사실과 외삽 불확실성을 올바르게 예측한다는 사실은 별개다. 전체 PP-X와 Engression에 대한 새 비교는 이번 단계에서 수행하지 않았다.

다음 작업을 한다면 신경망을 확장하기보다 회귀형을 기준으로, TRAIN 내부의 추가 제외 유닛/시간 구간에서 **관계 예측 잔차와 함수 오지정을 구분해 불확실성을 추정**하는 편이 타당하다. 현재 query 결과를 보고 구간 폭만 늘려 성과로 보고하지 않는다. 이 후속 개선은 이번 구현에서 완료한 작업이 아니다.

## 산출물

- 실행: `experiments/prefix_relation_encoder_screen.py`
- 모델: `src/pp_extrapolation/prefix_relation_encoder.py`
- 검증: `experiments/verify_prefix_relation_encoder.py`
- 결과·고정 프로토콜·체크포인트: `results/prefix_relation_encoder_train_v1/`
- 테스트: `tests/test_prefix_relation_encoder.py`

기존 함수 공유·진단 결과와 PP-X 기본 모델은 수정하지 않았다. 보존된 후보 중 회귀형은 정확도 개선을 연구할 기준선이고, 신경망 주 후보는 미채택이다.
