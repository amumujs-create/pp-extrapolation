# 관측별 성분 신뢰도: 구현 및 판정

## 판정

관측 이력의 길이·외삽 거리·계수 불확실성에 따라 수준/기울기/곡률 신뢰도를 학습하는 구조를 구현했다. 그러나 성분별 신뢰도가 성분 공통 신뢰도보다 점예측에서 우수하지 않았고, 명목 90% 구간 coverage에도 미달했다. 따라서 이 구조를 후속 모델의 핵심 기여로 채택하지 않는다. PP-X 기본 모델은 유지하며, 외부 PP-X/Engression 비교 단계로 넘어가지 않았다.

이 판정은 제안한 특정 구조와 고정 학습 설정에 대한 결과다. 가능한 모든 성분별 모델이 실패한다는 증명은 아니다. 새 구조의 우위를 뒷받침하는 실험 근거가 없다는 의미다.

## 실제 구현

기존 관측 이력 회귀형이 제공하는 함수계수 Gaussian과 소스 유닛 모집단의 함수계수 Gaussian을 결합한다. 각 계수는 로그 역속도 함수의 a,b,c이고, RUL은 그 양의 함수를 건강 경계까지 적분한다. 관측에 따른 신뢰도 w는 0~1 범위다.

`mean = w * ridge_mean + (1-w) * population_mean`

`covariance = diag(w) ridge_cov diag(w) + diag(1-w) population_cov diag(1-w) + disagreement`

disagreement는 `w*(1-w)*(ridge_mean-population_mean)^2`의 대각행렬이다. 신뢰도가 낮을 때 모집단 분포를 사용하고, 두 추정의 불일치를 분산에 남긴다. 이것은 단일 Gaussian 근사다. 모든 가능한 다봉 관계를 보존하거나, 관측으로 식별 불가능한 관계를 정확히 판별하는 구조는 아니다. 신뢰도가 낮을수록 분산이 반드시 커지는 단조 보장도 없다.

게이트 입력은 현재까지 관측 수의 로그, 소스 건강 범위 대비 외삽 거리, 회귀형/모집단 계수 표준편차 비율이다. prefix 길이는 인과적으로 계산되며 query 정답을 읽지 않는다. 길이에 대한 신뢰도 단조 증가 제약은 두지 않았다. 길이·거리만으로 실제 물리적 건강 상태를 식별한다고 주장하지 않는다.

대조군은 회귀형 그대로(w=1), 고정 신뢰도(w=0.5), 모든 계수에 같은 신뢰도를 사용하는 공통 게이트, 성분별 게이트다. 공통 게이트는 4개, 성분별은 12개 학습 파라미터이며 동일 자유도 비교는 아니다. 게이트 이외 회귀형·bank는 고정한다.

## 학습/평가

MICH TRAIN의 3개 유닛 fold × 건강 cutoff 0.6/0.8을 사용한다. 각 외부 donor 안에서 다시 동일한 6개 내부 분할을 만든다. 내부 소스와 내부 평가 유닛은 분리되고, 소스 건강 구간 전체가 평가보다 높다. 외부 query 유닛은 내부 학습/평가 어디에도 들어가지 않는다.

게이트는 내부 평가의 energy loss를 최소화하도록 120 step 학습한다. seed는 42,43,44이며 step 수·학습률·정규화는 외부 query로 선택하지 않았다. 학습된 게이트를 외부 donor에서 적합한 회귀형과 bank에 적용한다. 6개 외부 에피소드는 이전 개발에서도 사용되었고 서로 겹치므로 새 독립 확증 자료가 아니다. VAL·테스트는 읽지 않았다.

주 후보 채택 조건은 모든 대조군보다 평균 유닛 MSE 2% 이상 개선, coverage 90% 이상, interval score가 회귀형보다 좋음, 최악 유닛 MSE가 회귀형보다 나쁘지 않음이었다. 조건을 통과하지 못했다.

## 사전 고정 256표본 평가

유닛 MSE는 에피소드 내부 유닛별 MSE 평균을 구한 후 6개 에피소드를 평균했다. 구간 지표는 에피소드 내부 행 평균 후 6개 평균이다. 학습 모델은 3개 seed의 분포를 합친 혼합분포, 정적 대조군은 1개 분포다.

| 후보 | 유닛 MSE ↓ | 90% 구간 coverage | 평균 폭 | interval score ↓ |
|---|---:|---:|---:|---:|
| 회귀형 | 0.339752 | 62.98% | 1.012716 | 2.664176 |
| 고정 신뢰도 | 0.335261 | 69.33% | 1.156070 | 2.516390 |
| 성분 공통 신뢰도 | 0.310628 | 61.23% | 0.974978 | 2.693751 |
| 성분별 신뢰도: 주 후보 | 0.325082 | 68.54% | 1.149021 | 2.401222 |

회귀형 대비 주 후보 평균 오차가 약 4.3% 줄었지만 공통 게이트보다 나쁘다. 공통 게이트는 점예측 개선과 동시에 구간 지표 악화를 보인다. 주 후보도 interval score가 개선됐을 뿐 coverage는 목표 미달이다. 단일 지표 개선으로 승격하지 않았다.

## 수치 적분 점검

이전 회귀형의 128개 난수 표본 점수(0.309736)와 이번 256개 점수(0.339752)가 달라, 표본 수치 오차를 별도 확인했다. 서로 다른 표본 수의 점수를 직접 연결해 모델 개선량이라고 주장하면 안 된다.

모든 파라미터를 고정한 뒤 scrambled Sobol 정규 표본 512개와 2,048개로 사후 재평가했다. 이 점검은 모델 재학습·재선택이 아니며, 사전 고정 판정 결과를 대체하지 않는다. 두 표본 수 사이 MSE 변화는 각 후보에서 0.0004 이하였다. 정확한 적분 오차 상한을 증명한 것은 아니다.

| 후보 | 2,048표본 MSE ↓ | coverage | interval score ↓ |
|---|---:|---:|---:|
| 회귀형 | 0.343844 | 65.36% | 2.619381 |
| 고정 신뢰도 | 0.346280 | 71.23% | 2.515621 |
| 성분 공통 신뢰도 | 0.317981 | 63.40% | 2.653969 |
| 성분별 신뢰도 | 0.329727 | 70.70% | 2.352041 |

수치 점검에서도 성분별 게이트의 공통 게이트 대비 점예측 열세와 coverage 부족은 그대로다. 따라서 실패 판정은 최초 Monte Carlo 표본 한 묶음의 우연만으로 설명되지 않는다.

## 연구 결정

‘성분마다 다르게 믿으면 모델링 기여가 된다’는 가설은 이번 실험으로 지지되지 않았다. 관측별 게이트가 적합성·식별성을 정확히 추정한다는 이론적 근거도 제시하지 않았다. 이 구조를 신규성 확보 모델이라고 부르지 않는다.

앞으로도 이 동일 외부 TRAIN 구간을 반복해서 튜닝하면 개발 과적합 위험이 커진다. 다른 게이트를 계속 추가하기보다 이번 후보를 미채택으로 기록하고, 기존 단순 회귀형/구간 보정과 함께 고정된 비교 대상으로 남기는 것이 맞다. 실제 PP-X·Engression 초과는 별도의 일관된 외부 비교를 통과해야 한다.

## 파일 및 확인

- 모델: `src/pp_extrapolation/component_reliability.py`
- 실행: `experiments/component_reliability_screen.py`
- 재생 검증: `experiments/verify_component_reliability.py`
- 수치 점검: `experiments/component_reliability_quadrature.py`
- 결과: `results/component_reliability_train_v1/`
- 테스트: `tests/test_component_reliability.py`

관련 테스트 14개 통과. 저장 체크포인트 48개 예측 재생, 내부 유닛/건강 분리 36개, 결과 지표 재계산을 통과했다. 기존 PP-X 코드·과거 결과는 변경하지 않았다.
