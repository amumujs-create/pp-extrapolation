# 함수 프라이어 TRAIN 내부 진단

## 결론

현재 관측에 신호가 전혀 없는 것은 아니다. 그러나 단순 유사도는 외삽에 가장 잘 맞는 donor를 잘 찾지 못했고, 초기 구간에서 추정한 함수 자체도 후반 구간으로 안정적으로 전달되지 않았다. 따라서 공유 가중치만 개선하면 해결된다고 결론내릴 수 없다. 함수 추정의 구간 안정성과 관측→관계 예측을 함께 다루는 것이 다음 개발 방향이다. 이번 작업은 진단이며 새 모델 승격·PP-X 수정·Engression 초과 검증이 아니다.

## 범위와 누출 구분

기존 MICH TRAIN 18유닛만 읽었다. VAL 및 테스트 행·체크포인트는 읽지 않았다. 3개 유닛 fold × 2개 건강 cutoff(0.6/0.8 quantile)의 기존 6개 에피소드를 사용했다. donor는 다른 유닛의 높은 건강 구간, query는 제외 유닛의 낮은 건강 구간이다. 에피소드가 겹치므로 독립 6회 실험이나 유의성 검정으로 취급하지 않는다.

두 가지 사후 참고값을 별도로 계산했다. ‘자기 유닛 전체 함수 적합’은 query의 RUL 정답을 포함한다. ‘최적 donor’는 query 정답을 보고 단일 donor를 고른 값이다. 둘 다 실사용 예측이 아니다. ‘자기 유닛 초기 함수 적합’도 신규 유닛의 초기 RUL 정답을 사용하므로, RUL 정답이 없는 신규 유닛에 곧바로 적용할 수 없는 진단용 비교다.

공유기는 이전 VAL로 선택한 파라미터가 아니라 초기 고정 유사도 척도를 사용했다. 따라서 이전 학습 공유기 성능의 재평가가 아니다. bank 재적합은 실제 TRAIN→TRAIN+VAL 대신 TRAIN 내부 donor의 초기→전체 구간으로 모사했다. 이때 추가되는 donor 후반 정답은 재적합 변화 확인에만 사용한다.

## 1. 함수 추정 자체의 구간 안정성

같은 평가 행에서 자기 유닛 함수를 비교한 정규화 RUL 유닛 MSE의 에피소드 평균:

| 적합에 사용하는 자기 유닛 정답 | 뒤 구간 MSE |
|---|---:|
| 초기 구간만 | 0.452181 |
| 전체 구간: 사후 참고값 | 0.103880 |

초기 적합 오차가 전체 적합보다 약 4.35배다. 선택한 정규화·목적함수·함수족에서 초기 적합이 후반 관계를 충분히 정하지 못한다는 증거다. 함수족 자체의 표현력 한계, 정규화 편향, 추정의 불안정성을 아직 분리하지 못했으며, ‘본질적 비식별성’을 증명한 것은 아니다.

전체 TRAIN의 자기 유닛 적합 MSE도 0.233756으로 0이 아니다. 따라서 잘 맞는 함수족이 이미 확보되었다고 말할 수 없다. 이 수치는 로그 RUL 목적함수로 적합한 결과를 원래 정규화 RUL MSE로 평가한 값이라 목적함수 차이도 고려해야 한다.

fold 2 / cutoff 0.6에서 유닛 9는 초기 구간 행이 없어 자기 초기 함수 비교에서 제외했다. 해당 에피소드는 6유닛 265행 중 5유닛 248행만 비교했고, 전체 함수 참고값도 동일 행으로 맞췄다. 다른 에피소드는 전부 비교 가능했다. 누락을 다른 유닛 프라이어로 대체하거나 숨기지 않았다.

## 2. 유사도와 실제 donor 적합성의 차이

아래는 모든 query 행을 사용하는 정규화 RUL 유닛 MSE다. 계수 평균 후 함수를 계산하는 것과 함수값을 평균하는 것은 비선형성 때문에 다르므로 별도로 표시했다.

| 방식 | MSE |
|---|---:|
| donor 함수값 균일 평균 | 0.490371 |
| donor 계수 균일 평균 | 0.390394 |
| 현재 행의 관측 특징으로 가장 가까운 donor | 0.338909 |
| 초기 고정 척도의 분포 공유기 | 0.422310 |
| 정답을 보고 유닛별 최적 donor 선택: 사후 참고값 | 0.075545 |

단순 관측 유사도에도 평균 대비 개선 신호는 있다. 하지만 최적 donor와의 차이는 크다. 유닛 내 평균 관측 거리가 가장 가까운 donor와 실제 뒤 구간 MSE가 최소인 donor는 36개 중복 포함 유닛–에피소드 비교에서 한 번도 일치하지 않았다. 이 일치율은 행별 최근접 예측과 별개의 유닛 단위 진단이고, 유한 donor 집합의 정답 기반 최솟값은 낙관적인 참고값이다.

따라서 ‘비슷한 관측이면 동일한 외삽 관계’라는 가정은 현재 거리 척도에서 약하다. 정답 기반 최적 donor 성능을 달성 가능 성능이나 보장된 개선 폭으로 해석하지 않는다.

## 3. 관측 특징에 관계 예측 신호는 있는가?

유닛 초기 구간의 7개 평균 특징으로 해당 유닛의 전체 적합 계수 a,b,c를 예측하는 고정 ridge(alpha=1) probe를 추가했다. 한 유닛씩 제외하고, 정규화와 회귀 적합 모두 나머지 유닛만 사용했다. 하이퍼파라미터 선택은 하지 않았다. 비교는 나머지 유닛 계수 평균이다.

| 초기 구간 cutoff | a MSE 비율 | b MSE 비율 | c MSE 비율 |
|---|---:|---:|---:|
| 0.6 | 0.7796 | 1.0884 | 0.8645 |
| 0.8 | 0.6356 | 0.5002 | 0.7303 |

1 미만이면 평균보다 좋다. 더 긴 초기 구간에서는 세 성분 모두 개선된다. 따라서 ‘관측으로 관계를 전혀 구분할 수 없다’는 결론은 맞지 않는다. 다만 짧은 구간에서는 b가 평균보다 나쁘다. 이 probe는 cutoff별로 한 번의 LOOU 비교이며 세 fold에 같은 값이 반복되어도 3회 독립 증거가 아니다. 추정된 계수가 표적이므로 실제 물리 계수의 회복이나 외삽 RUL 성능 개선을 입증한 것도 아니다.

관측 열화율의 음의 로그와 소스 함수의 로그 역속도 사이 원시 상관은 에피소드별 약 -0.217~0.165였다. 기존 제약된 보정 신뢰도는 모두 0이다. 즉, 7개 특징 전체의 신호와 단일 열화율 proxy의 신호를 구별해야 한다. 단일 proxy가 약하다는 이유로 전체 특징을 버릴 근거는 없다.

## 4. bank 재적합은 무엇을 바꾸는가?

공유 척도를 고정한 채, 동일 donor 유닛의 초기 구간 bank를 전체 구간 bank로 바꾸었다. 정규화 RUL 유닛 MSE:

| 교체 항목 | MSE |
|---|---:|
| 교체 없음 | 0.422310 |
| 함수 평균·공분산만 | 0.196467 |
| donor 특징·정규화만 | 0.491367 |
| 관측 보정만 | 0.447051 |
| 전체 bank | 0.202878 |

함수 평균·공분산을 바꿀 때 예측 이동 RMS가 각 에피소드에서 가장 컸다. 이 모사에서는 추가 구간 정답을 이용한 함수 재추정이 개선을 주도하고, 특징·보정만 교체하면 평균 오차는 오히려 커졌다. 상호작용이 있으므로 각 차이를 가산적인 원인 기여도로 해석할 수 없다. 실제 TRAIN→VAL 실패의 원인을 확정하는 실험도 아니다.

## 다음 개발의 구체적 방향

후보를 하나 더 만든다면 ‘현재 값이 비슷한 donor’가 아니라 **관측 prefix로 이후의 관계 분포를 예측하는 인코더**를 먼저 시험하는 것이 타당하다. 짧은 prefix의 계수를 확정된 정답처럼 강하게 공유하지 않고, 여러 prefix에서 얻은 관계 추정과 뒤 구간 예측을 함께 학습해 구간에 따른 불안정성을 다뤄야 한다.

공정한 다음 통제군은 균일 공유, 고정 거리, 위 ridge 관계 예측이다. 학습·checkpoint 선택·최종 재적합에 같은 prefix→관계 구성을 유지하고, 새 테스트를 열기 전에 TRAIN 내부 제외 유닛·뒤 구간에서 통제군을 이기는지 확인해야 한다. 이것은 후속 개발 제안이며 이번 진단에서 구현하거나 성능을 확인한 모델이 아니다. 새로운 학술적 기여라는 주장도 아직 하지 않는다.

## 재현성과 정정 기록

유효한 최종 결과는 `results/function_prior_train_diagnosis_v3/`다. v1은 signed coordinate cutoff를 건강 cutoff로 잘못 사용하여 자기 초기 적합과 probe 범위가 잘못되었다. v2에서 수정했으나 초기 행이 없는 유닛 때문에 중단됐다. v3는 초기/후반 분리를 assertion으로 검사하고 누락 범위를 보고한다. v1/v2는 감사 추적용으로 보존하며 결과 근거로 쓰지 않는다. 기존 함수 공유 실험 코드와 그 결과는 변경하지 않았다.

실행: `experiments/function_prior_train_diagnosis.py`

검증: `experiments/verify_function_prior_train_diagnosis.py`

테스트: `tests/test_function_prior_train_diagnosis.py`

재생 검증은 데이터·코드 hash, 6개 구간/유닛 분리, 12개 bank 재적합, 저장 점수 및 oracle 선택을 확인한다. 코드 검증 통과가 성능·노벨티 입증을 뜻하지 않는다.
