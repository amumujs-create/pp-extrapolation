# DS03 seed 확장·Engression 비교·노벨티 대조 검증

2026-09-13. 요청한 추가 확인을 실행했다. **기존 PP-X 대비 제한적 개선은 재현됐지만 Engression 우위는 확인되지 않았다. 성분 분리의 효용은 일부 지지되며, 교차적합 자체의 추가 효용은 지지되지 않는다. 후속 모델로 승격하지 않는다.**

## 1. 이번에 실행한 범위

- 기본 PP-X seed: 42–46, 기존 3개에서 5개로 확장.
- 각 기본 모델에 대해 보정망 초기화: 42–46 전부 반복.
- 세 arm: 성분별 gate+교차적합, 공통 gate 하나+교차적합, 성분별 gate+교차적합 제거.
- 총 5×5×3 = **75개 보정망 적합**. 새로운 기본 seed 45/46의 TRAIN-only source 모형도 6개씩 추가 적합했다.
- 주 결과는 사전 고정된 대각 조합 `(base42,gate42), …, (base46,gate46)`의 5모델 ensemble이다. 잘 나온 초기화로 바꾸지 않았다.
- 별도로 동일 초기화를 공유하는 5개의 ensemble 결과를 모두 제시했다. 25개 조합은 독립적인 25개 cohort가 아니다.
- 기존 seed 42–44의 주 모델 validation 예측이 이전 실행과 재현되는지 확인했다.
- test 행 438개, test 유닛 6개, TRAIN/validation 분할과 PP-X·Engression의 5-seed archive는 그대로다.

이것은 이미 공개된 DS03 개발 endpoint에 대한 재현성과 기여도 점검이다. 새 잠금 cohort 검증이나 test cycle-support 외삽 확인은 아니다. 모델을 고르는 validation 규칙은 동일하게 유지했다.

## 2. Engression 비교: 아직 못 넘었다

아래는 모두 동일한 5-seed ensemble이다. 기존 3-seed 결과와 직접 비교해 seed 증가 자체를 알고리즘 개선으로 해석하면 안 된다.

| 모델 | R² | RMSE | 최악 유닛 RMSE | positive-R² 유닛 |
|---|---:|---:|---:|---:|
| 기존 full PP-X fallback | 0.881805 | 7.73216 | 10.91344 | 6/6 |
| Engression | **0.901323** | **7.06496** | **10.51031** | 6/6 |
| 성분별 gate+교차적합: 주 모델 | 0.884661 | 7.63816 | 10.75919 | 6/6 |
| 공통 gate 하나+교차적합 | 0.881805 | 7.73216 | 10.91344 | 6/6 |
| 성분별 gate+교차적합 제거 | 0.884906 | 7.63005 | 10.86713 | 6/6 |

주 모델은 PP-X 대비 전체 RMSE 약 **1.22%**, 최악 유닛 RMSE 약 **1.41%**를 줄였다. 하지만 Engression에는 전체 및 최악 유닛 RMSE 모두 밀린다. 표의 교차적합 제거 arm이 전체 RMSE에서 조금 더 좋다고 주 모델을 바꾸지 않았다.

주 모델의 validation 채택 수치는 다음과 같다.

- 유닛 균등 MSE 비율: 0.979567, 기존 대비 약 2.04% 개선.
- 승리 유닛: 2/3.
- 최대 개별 유닛 RMSE 비율: 1.003958.

기존의 `2% 초과 개선 / 60% 이상 승리 / 개별 RMSE 증가 5% 이하` 규칙을 통과하지만, 개선 폭은 기준에 가깝다. validation 통과를 새로운 분포에서의 안전 보장으로 표현하지 않는다.

## 3. 특정 seed 하나뿐인가?

주 비교에서는 seed 43과 45의 보정이 활성화돼 **2/5**다. 나머지는 기존 PP-X 예측을 유지한다.

| 기본 모델 seed | 초기화 42 | 43 | 44 | 45 | 46 |
|---|---|---|---|---|---|
| 42 | OFF | OFF | OFF | OFF | OFF |
| 43 | ON | ON | ON | ON | ON |
| 44 | ON | ON | OFF | ON | ON |
| 45 | ON | OFF | OFF | ON | ON |
| 46 | OFF | OFF | OFF | OFF | OFF |

이는 주 모델의 validation checkpoint에서 보정이 활성화됐는지이며, ON을 test 승리와 동일시하지 않는다. 기본 모델 43에서는 초기화를 바꿔도 보정이 활성화되지만, 기본 모델 42/46은 전부 꺼진다. 따라서 초기화 우연만의 문제로 축소하기도 어렵고, 모든 기본 모델에서 안정적으로 효과가 있다는 주장도 할 수 없다.

초기화를 고정하고 기본 모델 5개를 평균한 별도 ensemble의 **guarded** R²는 아래와 같다. 각 ensemble의 validation gate도 따로 계산하되, 어떤 초기화도 사후 채택하지 않는다.

| 보정망 초기화 | R² | 정책 채택 |
|---|---:|---|
| 42 | 0.885949 | 통과 |
| 43 | 0.885377 | 통과 |
| 44 | 0.881805 | 거절, PP-X 유지 |
| 45 | 0.886831 | 통과 |
| 46 | 0.887396 | 통과 |

다섯 초기화 중 네 ensemble에서 PP-X 개선이 남지만 전부 Engression 0.901323보다 낮다. 46의 결과를 골라 주 성적으로 발표하지 않는다.

## 4. 구조와 학습 방법의 추가 기여

### 성분별 gate 대 공통 gate

공통 gate는 같은 8개 입력과 같은 8→12→3 네트워크에서 세 logit의 평균을 취해 동일 가중치 하나를 적용한다. 두 모델 모두 할당 파라미터는 147개이며 학습 목적·optimizer·epoch·validation 규칙이 같다. 다만 공통 gate의 유효 함수 차원은 더 낮으므로 완전한 표현 용량 일치는 아니다.

공통 gate는 25개 조합 모두 correction-off를 선택했다. 성분별 gate에서는 개선이 남았다. **현재 데이터에서 보정 성분을 독립적으로 조절하는 것이 공통으로 축소하는 것보다 유용하다는 증거**다. 이것은 세계 최초라는 증명이 아니다.

### 교차적합을 제거한 대조

donor/query 행은 같게 두되, query 유닛과 뒤쪽 support까지 포함한 전체 TRAIN으로 학습한 base의 잔차로 source 학습을 만들었다. 이 arm의 source 오차는 독립적인 held-out 추정이 아니며, 해당 대조는 테스트 정답을 사용한 것이 아니다. 외부 validation/test 분리는 그대로다.

이 대조의 주 결과 R² 0.884906은 교차적합 0.884661과 비슷하고, 전체 RMSE는 오히려 조금 낮다. **현재 교차적합 규칙을 모델의 핵심 성능 기여로 내세울 근거는 확보되지 않았다.** 비교에서 바뀌는 것은 base의 source 학습 범위와 이에 따른 잔차 분포 전체이므로 교차적합의 통계적 필요성이 일반적으로 없다는 뜻도 아니다.

## 5. 불확실성 점검

test 유닛을 묶음으로 재표집하는 paired unit bootstrap 10,000회를 계산했다. 통계량은 유닛별 log RMSE 비율의 평균이며, 위 표의 pooled RMSE 비율과 다른 통계량이다. 음수는 주 모델이 좋다는 뜻이다.

| 비교 대상 | 평균 유닛 log RMSE 비율 | 탐색적 95% 구간 |
|---|---:|---|
| 기존 PP-X | -0.01842 | [-0.03490, -0.00628] |
| Engression | +0.23201 | [+0.03793, +0.44007] |
| 공통 gate 대조 | -0.01842 | [-0.03490, -0.00628] |
| 교차적합 제거 대조 | +0.00265 | [-0.01132, +0.01698] |

고정된 학습 모델에 조건부인 **한 cohort의 6개 유닛** 결과다. 반복적인 개발·모델 탐색의 영향을 교정한 확증적 유의성 분석이 아니며, 독립 cohort 일반화를 보장하지 않는다. Engression 비교 구간이 양수인 것은 이 점검에서도 주 모델에 불리하다.

## 6. 선행연구와 노벨티 주장 범위

초록만이 아니라 관련 본문 절을 확인했다. 다음은 관련 방법을 구분한 것이며 전체 선행문헌에 대한 완전한 우선권 증명은 아니다.

| 선행연구 | 이미 다룬 핵심 | 현재 구현에서 주장할 수 없는 것 |
|---|---|---|
| [Engression, §2–3](https://arxiv.org/html/2307.00835v3) | pre-additive noise 모델과 분포 적합을 결합하고, 가정 아래 외삽을 분석 | 단순히 노이즈·프라이어를 여러 개 둔다는 아이디어만으로 동등한 외삽 식별성 확보 |
| [Cross-Fitted Residual Utility, §IV–V](https://arxiv.org/html/2608.02063v1) | OOF 잔차 효용, 기본 예측 유지 선택, validation에서 고정한 보정 정책 | 교차적합+안전 보정+기본 모델 유지라는 조합 자체의 독창성 |
| [Transfer Learning and Locally Linear Regression, §4](https://arxiv.org/html/2511.12948v2) | source/target 함수 차이를 국소적으로 보정하고, smooth bias와 전이 오차를 분석 | 국소선형 잔차 전이 자체의 독창성 |

위 방법들과 현재의 세 성분 분해·유닛/구간 제외 학습이 완전히 동일하다고 판정한 것은 아니다. 다만 공통 요소가 많고, 현 대조에서 교차적합의 추가 성능 기여가 확인되지 않았다. 따라서 현재 후보에 대해 **방법론적 노벨티 확보**라고 결론낼 수 없다.

또한 현재 모델은 세 개의 수치적 잔차 성분을 학습해 재가중할 뿐, 사용자가 논의한 **프라이어 함수 분포 자체**, **프라이어 성분 간 공유 관계**, **관측에서 식별되지 않는 외삽 관계의 분포 유지**를 구현한 모델이 아니다. 그것을 구현했다고 이름만 바꾸어 설명해서는 안 된다.

## 7. 판정

1. 특정 seed 하나에서만 우연히 보정된다는 우려는 일부 완화됐다. 하지만 주 모델 2/5 활성화와 초기화 의존성이 남는다.
2. Engression 초과는 실패했다. 5개 초기화 slice를 모두 봐도 결과는 같다.
3. 성분 분리에는 제한적 실험 근거가 생겼다. 교차적합 학습의 고유 기여 및 노벨티는 입증되지 않았다.
4. 기존 PP-X를 교체하지 않는다. 새로운 잠금 cohort를 소모해 승격을 시도할 단계가 아니다.

이 결과에서 안전 기준을 완화하거나 초기화 46만 고르는 것은 해결이 아니다. 현재 게이트를 더 튜닝하는 것과 사용자가 원하는 새 외삽 방법론을 만드는 것을 구분해야 한다. 다음 구조 변경이 필요하다면 아직 구현되지 않은 함수 관계 분포와 공유 규칙을 명시적으로 모델링하는 별도의 가설이어야 하며, 이 실험은 그 성공 증거가 아니다.

## 8. 산출물과 검증

- 모듈: `src/pp_extrapolation/component_ablation.py`
- 실행: `experiments/component_seed_ablation.py`
- 검증: `experiments/verify_component_seed_ablation.py`
- 테스트: `tests/test_component_ablation.py`
- 결과: `results/component_seed_ablation_ds03_v1/`

모든 모델·validation/test 예측을 저장했다. **75개 모델 재실행, 18개 정책 비교, 4개 불확실성 비교 재계산, baseline 5개 및 Engression archive 정렬 검증을 통과했다. 관련 테스트 41개도 통과했다.** 기존 모델과 결과는 덮어쓰지 않았다.
