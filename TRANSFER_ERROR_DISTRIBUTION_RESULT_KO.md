# 내부 외삽 오차 분포를 이용한 개선

## 결론

회귀형의 점예측을 유지하고 TRAIN 내부 외삽 잔차로 구간을 구성하는 대조군에서 구간 예측 개선을 확인했다. 평균 90% 구간 coverage는 60.04%에서 97.50%, interval score는 2.68285에서 2.25077로 약 16.1% 개선됐다. 다만 평균 구간 폭은 0.92869에서 2.23075로 약 2.40배다. 정밀한 불확실성 추정이 완성됐거나 외삽 coverage가 보장된 것은 아니다.

외삽 거리로 예측 중심까지 교정하는 주 후보는 점예측을 악화시켰고, 사전 채택 조건을 통과하지 못했다. 기존 PP-X는 교체하지 않았다. 보존할 개발 결과는 평균을 유지하는 오차 분포 대조군이며, 새 외부 검증 없이 최종 후보로 승격하지 않는다.

## 구현과 누출 방지

기존 MICH TRAIN의 6개 외부 유닛/건강 구간 분할을 유지했다. 각 외부 donor 안에서 다시 3개 유닛 fold × 2개 건강 cutoff로 6개 내부 분할을 만들었다. 총 36개 내부 분할마다 함수 bank와 회귀형을 새로 적합하고, 제외된 내부 유닛의 더 낮은 건강 구간에서 `log(RUL / 예측 평균)`을 수집했다.

외부 query 유닛은 해당 내부 학습·오차 수집 어디에도 들어가지 않는다. 각 단계에서 소스 건강 최솟값이 평가 건강 최댓값보다 큰지 확인했다. 외부 VAL·테스트는 읽지 않았다. 다만 동일 TRAIN 외부 에피소드를 이전 개발에서도 관찰했으므로 독립적인 확증 실험은 아니다.

관계 계수 분산을 추가로 크게 만드는 대신, 내부에서 관측된 예측 오차의 경험적 곱셈 분포로 기존 계수 불확실성 분포를 교체했다. 유닛 가중치를 동일하게 주고 256개 결정적 분위점으로 적분한다. 이 단계는 새로운 성분별 프라이어 공유 원리가 아니라 오차 분포 보정이다.

세 변형을 사전 고정했다.

- spread_only: 원시 로그 오차의 경험적 곱셈 요인을 산술 평균 1로 정규화한다. 기존 점예측을 정확히 보존한다. 단순 대칭 폭 확대와 달리 오차 분포의 비대칭 형태를 반영하지만 여전히 구간 보정이다.
- global: 같은 원시 오차 분포를 평균 정규화 없이 적용한다. 점예측도 움직인다.
- conditional: 학습 건강 구간까지의 정규화 거리로 로그 오차의 위치를 ridge 회귀하고, 내부 유닛 하나씩 제외한 회귀 잔차를 합성한다. 점예측은 합성 분포의 산술 평균이다.

로그 잔차와 조건부 위치는 각각 ±log(4)로 제한했다. 조건부 회귀 페널티는 0.1로 고정했다. 외부 query 결과에 맞춰 제한값·분위수·폭을 조정하지 않았다. 외삽에서 교환가능성이나 분포 불변성이 확인되지 않았으므로 conformal 보장 또는 90% 보장이라고 부르지 않는다.

## 결과

각 에피소드에서 유닛 MSE를 계산해 6개를 평균했다. coverage·폭·interval score는 에피소드 내부 행 평균을 계산한 뒤 6개를 평균했다. 단위는 정규화 RUL이다. 6개 에피소드는 서로 겹친다.

| 방식 | 유닛 MSE ↓ | 90% 구간 coverage | 평균 폭 ↓ | interval score ↓ |
|---|---:|---:|---:|---:|
| 기존 회귀형 계수 분포 | 0.309736 | 60.04% | 0.928690 | 2.682848 |
| **점예측 유지 오차 분포** | **0.309736** | **97.50%** | **2.230746** | **2.250771** |
| 전역 오차 교정 | 0.501617 | 92.55% | 2.291620 | 2.503852 |
| 거리 조건부 오차 교정: 주 후보 | 0.571904 | 93.15% | 2.417260 | 2.579147 |

90% interval score는 `폭 + 20×하한 미달 오차 + 20×상한 초과 오차`다. 넓히기만 하면 폭 벌점이 증가한다. 점예측 유지 대조군은 폭 증가에도 불구하고 과도한 미포함 벌점 감소로 총점이 좋아졌다. 다만 97.5%는 명목 90%보다 상당히 높아 보수적이며 모든 유닛·외삽 거리에서 90%를 달성했다는 뜻은 아니다.

주 후보는 평균 MSE가 약 84.6% 악화했다. 한 외부 에피소드에서는 1.0271에서 2.7702로 악화했다. 내부 오차의 중심이 더 먼 외부 구간에도 그대로 전달된다는 가정을 지지하지 못한다. 점예측 유지 대조군은 MSE를 바꾸지 않으므로 새 정확도 개선으로 보고하지 않는다.

## 채택 범위

이전 회귀형의 정확도 개선은 유지할 수 있고, 그 구간 추정에는 새 개발 기준선이 생겼다. 그러나 이는 고정 유사도 대비 TRAIN 내부 개선이지 전체 PP-X·Engression 대비 승리가 아니다. DS03 적용 가능성, 전체 데이터 커버리지 및 학술적 노벨티도 확인하지 않았다.

다음 검증에서는 점예측 유지 변형을 미리 고정한 뒤, 아직 사용하지 않은 검증 범위에서 폭·유닛별 coverage·interval score를 확인해야 한다. 현재 결과만으로 기본 예측 파이프라인에 적용하지 않는다.

## 산출물

- `src/pp_extrapolation/transfer_error_distribution.py`
- `experiments/transfer_error_distribution_screen.py`
- `experiments/verify_transfer_error_distribution.py`
- `tests/test_transfer_error_distribution.py`
- `results/transfer_error_distribution_train_v1/`

기존 모델·결과 파일은 변경하지 않았다. 구간 구성 재현성, 유닛/구간 분리, 점예측 유지 성질은 별도 검증한다.
