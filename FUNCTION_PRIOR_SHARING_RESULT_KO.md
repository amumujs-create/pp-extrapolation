# 함수 프라이어 성분별 공유: 구현 및 외삽 평가

## 결론

유닛별 함수 분포를 만들고, 성분별 공유 규칙을 외삽 전달 손실로 학습하는 후보를 구현했다. 그러나 이번 고정 후보는 채택하지 않는다. MICH에서 Engression보다 좋지만 전체 PP-X보다 나쁘고, 학습 공유가 고정 공유보다 우수하지 않았다. 노벨티와 성능을 입증한 후속 모형으로 발표할 근거는 아직 없다. 기존 PP-X 기본 동작은 변경하지 않았다.

## 구현한 구조

건강 경계까지의 정규화 거리 m에 대해 유닛별 함수는 다음과 같다.

`log slowness(h) = a + b*h + c*h²`

`RUL(m) = integral[0,m] exp(a + b*h + c*h²) dh`

각 유닛의 a, b, c를 로그 RUL 관측으로 추정하고 정규화된 Laplace 근사 공분산을 만든다. a, b, c는 로그 역속도의 수준·기울기·곡률 계수이며, 실제 열화 속도와 가속도를 직접 측정한 값은 아니다. 알려진 건강 경계, 양의 관측 열화율, 매끄러운 건강–수명 관계를 가정한다. 수치 계산에서는 지수 입력을 [-8, 8]로 제한한다.

각 성분은 서로 다른 유사도 척도로 소스 유닛을 참고한다. 7개 관측 특징에 대한 성분별 척도 21개를 학습한다. 소스 유닛 내부의 계수 불확실성과 유닛 간 계수 차이를 공유 공분산에 반영한다. 최종 분포는 단일 Gaussian 근사이므로, 가능한 모든 다봉 관계를 그대로 보존하는 구조는 아니다.

현재 관측의 열화율을 함수 값에 연결하는 보정식을 소스에서 학습하고, 신뢰도에 따라 rank-one Gaussian 조건부 갱신을 적용한다. 신뢰도가 0이면 갱신하지 않는다. 이는 경험적 근사이며 인과 식별성 보장이 아니다. 한 계수 표본이 전체 함수를 정의하므로 같은 관측 문맥에서는 구간별 예측이 일관된다. 문맥을 바꾸어 다시 조건화한 예측까지 하나의 고정 경로라고 주장하지 않는다.

## 학습·검증 설계

- MICH TRAIN 18유닛 1,924행, VAL 6유닛 706행. 테스트는 8유닛 202행이며 모두 TRAIN+VAL보다 낮은 건강 구간이다.
- TRAIN에서 3개 유닛 fold × 건강 cutoff 0.6/0.8로 6개 전달 에피소드를 만든다. donor는 다른 유닛의 앞 건강 구간, query는 제외된 유닛의 뒤 건강 구간이다. query 유닛·구간은 해당 donor 함수 추정에 들어가지 않는다.
- 분포 후보는 energy loss로 공유 척도를 학습한다. 분포 제거 후보에서는 손실이 MAE로 퇴화하므로 동일한 확률 목적함수 비교는 아니다.
- seed 42/43/44, 사전 고정 6개 후보. 정적 후보는 seed 간 동일하므로 3회 독립 학습으로 세지 않는다.
- 체크포인트 선택에는 64개 표본 평균의 VAL 유닛 MSE, 외부 채택 판정에는 256개 표본 평균을 사용한다. 테스트는 선택 저장 후 평가했다.
- 채택 조건: VAL 유닛 MSE 비율 <0.98, 유닛 승률 ≥0.6, 최대 유닛 RMSE 비율 ≤1.05. 기준은 전체 PP-X다.
- 선택 후 공유 척도는 고정하고 함수 bank·관측 보정·특징 정규화를 TRAIN+VAL에서 다시 적합한다. 따라서 VAL에서 테스트로 넘어갈 때 분포 이동 가능성이 있다.
- 비교 PP-X는 기존 3개 배터리 공동 학습 결과, 신규 후보는 MICH 단독 학습이다. 동일 학습 풀·계산량의 통제 비교나 모델 구조의 인과 효과 추정은 아니다.
- 이전 개발에서 본 테스트를 다시 평가한 결과이며, 새 잠금 코호트 확인 실험이 아니다.

## 결과

3개 seed 예측 평균. 아래는 guard 적용 전 후보 자체의 결과이며 RMSE 단위는 cycle이다.

| 후보 | R² | RMSE | 최악 유닛 RMSE | R² > 0 유닛 |
|---|---:|---:|---:|---:|
| 전체 PP-X | 0.710033 | 8.6301 | 10.5841 | 8/8 |
| Engression | -3.492444 | 33.9691 | 81.0964 | 3/8 |
| 개별 유닛 계수 공유 없음 | 0.241468 | 13.9582 | 22.6577 | 6/8 |
| 균일 고정 공유 | 0.719726 | 8.4846 | 11.2154 | 7/8 |
| 성분 공통 척도 학습 | 0.512331 | 11.1919 | 12.7702 | 7/8 |
| **성분별 공유 학습: 주 후보** | **0.651195** | **9.4653** | **12.2009** | **8/8** |
| 관측 조건부 갱신 제거 | 0.631376 | 9.7305 | 12.6695 | 8/8 |
| 계수 분포 제거 | 0.528556 | 11.0042 | 14.7916 | 8/8 |

모든 후보가 VAL 채택 조건을 통과하지 못했다. 주 후보의 VAL 유닛 MSE 비율은 2.85064, 승률은 1/6, 최대 유닛 RMSE 비율은 2.40787이다. 고정 공유의 테스트 평균이 조금 좋다는 이유로 사후 선택하지 않는다. 고정 공유는 최악 유닛 오차와 양의 R² 유닛 수도 PP-X보다 나쁘다. guard가 반환하는 PP-X 결과는 개선 성과가 아니다.

90% 계수 예측구간의 경험적 행 coverage는 주 후보 81.68%, 평균 폭 25.925 cycle이다. 고정 공유는 85.64%, 폭 29.027이다. 명목 coverage를 만족하지 못했다. 이 구간은 명시적 관측 잡음을 포함하지 않으며 보정된 예측구간이라는 보장이 없다. R² 양수 유닛 비율과 예측구간 coverage는 다른 지표다.

## 관측으로 확인한 병목과 해석 한계

6개 소스 전달 에피소드 모두에서 열화율 보정 신뢰도가 0이었다. TRAIN 전체에서는 0.03459, TRAIN+VAL에서는 0.05701이었다. 즉, 선택한 함수족·비음수 기울기 보정 제약 아래에서는 관측 열화율과 추정된 함수 관계의 연결이 약했다. 전달 학습 중 관측 갱신이 유용하게 작동했다고 주장할 수 없다. 이것만으로 물리적 관계 자체가 식별 불가능하다거나 실패 원인이 하나뿐이라고 결론내리지 않는다.

개별 유닛 공유 없음 대조군도 소스의 전체 수준 평균과 관측 보정은 사용한다. 완전한 무프라이어 대조군은 아니다. 성분별 학습이 공통 척도보다 나았다는 사실만으로 노벨티를 입증할 수 없다. 고정 공유보다 나빠 핵심 공유 학습의 실증적 가치를 확보하지 못했다.

## 선행 연구와 노벨티 판정

선택적인 공분산 성분 공유 자체는 이미 연구되었다. [Tong & Choi, 2019](https://proceedings.mlr.press/v97/tong19a.html)는 여러 시계열에서 잠재 커널 성분의 선택적 공유를 다룬다. [Ho Yoon et al., 2022](https://proceedings.mlr.press/v151/ho-yoon22a.html)는 공유·태스크별·표본별 효과를 결합한다. 따라서 유닛별 관계와 공유 성분을 둔다는 발상 자체를 최초라고 주장할 수 없다.

미분 관측으로 함수 분포를 갱신하는 원리도 기존 연구 영역이다. [de Roos et al., 2021](https://arxiv.org/abs/2102.07542)은 미분 정보를 이용하는 Gaussian process 추론을 다룬다. 현재 Gaussian 조건부 갱신 자체는 새 원리가 아니다.

이번 후보의 연구 가설은 ‘미관측 유닛의 후반 건강 구간 전달 오차로 성분별 공유를 학습하고, 양의 일관된 함수 분포를 유지한다’는 결합이다. 위 문헌 확인은 완전한 선행 연구 조사가 아니며, 동일 방법 부재나 학술적 신규성을 보증하지 않는다. 현재 성능·대조 실험으로는 독립적인 기여를 입증하지 못했다.

## 범위와 다음 결정

이 함수 모형은 알려진 건강 경계와 양의 열화율이 필요하다. DS03에 cycle을 건강 변수로 대체하여 적용하지 않았다. DS03 fallback 개선이나 전체 데이터 커버리지 확보 결과가 아니다.

이 후보를 PP-X 후속 기본 모형으로 승격하지 않는다. 다음 개발 전에 TRAIN 내부에서 (1) 함수족 자체의 재현 오차, (2) 관측 특징이 유닛 함수 차이를 예측하는 정도, (3) TRAIN→VAL bank 재적합 이동을 분리 진단해야 한다. 그 확인 없이 프라이어·레짐·공유 층을 추가하거나, 이번 테스트 결과에 맞춰 공유 규칙을 재튜닝하지 않는다. 이 진단은 후속 제안이며 본 실험에서 완료한 작업은 아니다.

## 재현성 검증 및 산출물

- 모델: `src/pp_extrapolation/function_prior_sharing.py`
- 실행: `experiments/function_prior_sharing_screen.py`
- 검증: `experiments/verify_function_prior_sharing.py`
- 결과·선택·프로토콜·체크포인트: `results/function_prior_sharing_mich_v1/`
- 검증 결과: `results/function_prior_sharing_mich_v1/verification.json`

독립 검증에서 bank 재적합 7개, 소스 제외 확인 6개, 선택/최종 체크포인트 재생 36개, 분포 행렬 6개 및 평가 행렬 14개가 통과했다. 함수 경계와 공분산 양의 정부호도 확인했다. 관련 테스트 묶음은 48개 통과했다. 코드·재현성 검증 통과는 모델 성능이나 불확실성 보정의 성공을 의미하지 않는다.
